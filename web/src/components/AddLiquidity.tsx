import React from "react";

export default function AddLiquidity() {
  return (
    <div>
      <h2>Add Liquidity (placeholder)</h2>
      <button onClick={() => alert("Integrate router addLiquidity methods")}>Add Liquidity</button>
    </div>
  );
}
