import React from "react";

export default function StakeWidget() {
  return (
    <div>
      <h2>Stake PORK (placeholder)</h2>
      <p>Approve PORK → stake → claim rewards</p>
      <button onClick={() => alert("Integrate wagmi + contract calls")}>Stake</button>
    </div>
  );
}
